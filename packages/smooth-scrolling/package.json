{
  "name": "@wareme/smooth-scrolling",
  "description": "High performance smooth scrolling for Dark applications",
  "version": "3.2.0",
  "keywords": [
    "smooth-scrolling",
    "Dark"
  ],
  "author": "Coachonko",
  "repository": "https://github.com/Coachonko/wareme",
  "license": "NCSA",
  "main": "dist/index.js",
  "scripts": {
    "test": "bun test",
    "clean": "rm -rf ./dist",
    "build": "bun run clean && bun build ./src/index.js --outdir ./dist --minify --sourcemap=external --external @dark-engine/core --external @wareme/raf-nexus",
    "dev:build": "bun run clean && bun build ./src/index.js --outdir ./dist --external @dark-engine/core --external @wareme/raf-nexus",
    "dev": "bun run dev:build --watch"
  },
  "dependencies": {
    "@wareme/event-emitter": "workspace:*",
    "@wareme/utils": "workspace:*"
  },
  "devDependencies": {
    "standard": "^17.1.0"
  },
  "peerDependencies": {
    "@wareme/raf-nexus": "workspace:*",
    "@dark-engine/core": "^1.4.1"
  }
}