{
  "name": "scroll-to-top",
  "description": "Scroll to top on when pathname changes",
  "version": "3.0.0",
  "keywords": [
    "util",
    "scroll",
    "route",
    "pathname"
  ],
  "author": "Coachonko",
  "repository": "https://github.com/Coachonko/wareme",
  "license": "NCSA",
  "main": "dist/index.js",
  "scripts": {
    "test": "bun test",
    "clean": "rm -rf ./dist",
    "build": "bun run clean && bun build ./src/index.js --outdir ./dist --minify --sourcemap=external --external @dark-engine/core --external @dark-engine/web-router",
    "dev": "bun run build --watch"
  },
  "devDependencies": {
    "standard": "^17.1.0"
  },
  "peerDependencies": {
    "@dark-engine/core": "^1.3.0",
    "@dark-engine/web-router": "^1.3.0"
  }
}